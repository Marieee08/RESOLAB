datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model AccInfo {
  id         Int     @id @default(autoincrement())
  email      String  @unique
  name       String?
  contactNum String?
}
